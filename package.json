{
  "name": "bikerouter",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "leaflet": "^1.9.4"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.32.3",
    "vite": "^7.3.1",
    "wrangler": "^4.83.0"
  }
}
